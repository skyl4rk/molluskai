# MolluskAI Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   chmod 600 .env

# Required: Your OpenRouter API key
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Required: The model to use (OpenRouter model ID)
# Popular choices:
#   google/gemini-2.0-flash-001
#   anthropic/claude-3-5-haiku
#   openai/gpt-4o-mini
#   meta-llama/llama-3.3-70b-instruct
OPENROUTER_MODEL=google/gemini-2.0-flash-001

# Optional: Telegram bot token
# Create a bot with @BotFather on Telegram
TELEGRAM_TOKEN=

# Optional: Comma-separated Telegram user IDs allowed to use the bot
# Find your user ID by messaging @userinfobot on Telegram
# Example: TELEGRAM_ALLOWED_USERS=123456789,987654321
TELEGRAM_ALLOWED_USERS=

# Optional: Telegram chat ID for outbound task notifications (e.g. daily report)
# This can be the same as your user ID for personal use
TELEGRAM_CHAT_ID=

# Optional: Email gateway (IMAP polling + SMTP sending)
# Leave EMAIL_IMAP_HOST blank to disable the email gateway entirely.
#
# Gmail setup:
#   1. Enable IMAP in Gmail Settings → See all settings → Forwarding and POP/IMAP
#   2. Create an App Password at myaccount.google.com/apppasswords (requires 2-step verification)
#   3. Use the App Password below instead of your regular Gmail password
#
# Other providers: adjust HOST values (e.g. imap.outlook.com / smtp.office365.com)
EMAIL_IMAP_HOST=imap.gmail.com
EMAIL_IMAP_PORT=993
EMAIL_IMAP_USER=you@gmail.com
EMAIL_IMAP_PASSWORD=your-app-password

EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=you@gmail.com
EMAIL_SMTP_PASSWORD=your-app-password

# How often to check for new emails (seconds). Default: 60
EMAIL_POLL_INTERVAL=60

# Comma-separated list of sender email addresses allowed to message the agent.
# Leave blank to accept emails from anyone (appropriate for a customer-facing inbox).
# Example: EMAIL_ALLOWED_FROM=alice@example.com,bob@example.com
EMAIL_ALLOWED_FROM=
